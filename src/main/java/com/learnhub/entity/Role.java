package com.learnhub.entity;

/**
 * Enum representing the two roles in the system.
 * ADMIN - can manage categories, topics, and articles
 * USER  - can browse content and track progress
 */
public enum Role {
    ADMIN,
    USER
}
